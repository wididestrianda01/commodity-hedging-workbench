# local planning artifacts (wayfinder map, tickets, spec) stay out of the repo
.scratch/

# caches
__pycache__/
*.pyc
.ipynb_checkpoints/
.pytest_cache/
*.egg-info/

# Terraform working dir + state (state holds resource addrs; never commit)
pipeline/terraform/.terraform/
*.tfstate
*.tfstate.*

# local-only notes and planning artifacts (not for the public repo)
/interview-brief.md
/memos/

# market data bars regenerate locally (exchange-data licensing; manifests stay committed)
data/frozen/KC*.csv
data/frozen/GC*.csv

# latex build artifacts
report/*.aux
report/*.log
report/*.out
